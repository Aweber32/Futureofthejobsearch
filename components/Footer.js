export default function Footer(){
  return (
    <footer className="bg-light py-4 mt-5">
      <div className="container text-center text-muted">
        © {new Date().getFullYear()} Future of the Job Search — placeholder footer
      </div>
    </footer>
  )
}
